# Example PRD

## Overview

Create a simple utility function.

## Requirements

1. Create a `greet(name: string): string` function that returns "Hello, {name}!"
2. Add a test for the function
3. Export from an index file

## Acceptance Criteria

- Function handles empty string input
- Test covers happy path and edge cases
