# Review: 2f95a92

**Commit:** 2f95a9226f6e59b52207376c8331453a1439273a
**Message:** 📝 docs: add nested Claude execution documentation
**Date:** 2026-01-06 01:33:07 UTC

## Feedback

- docs mismatch: `delegateExecution`, `planInstructions`, and `RenderNodeToolInput/Result` are documented as API changes but aren’t in code (`src/core/types.ts` has no props; `src/core/nested-execution.ts` defines `RenderNodeResult`). Either implement these or mark the section as proposed/rename to match actual types. See `docs/nested-claude-execution.md:197`.
- Path generation example doesn’t match implementation: the docs use array index across all nodes and recurse over all children, but real code indexes per type and skips TEXT nodes. This yields different `node_path` values for mixed children, which would break `render_node` calls if users follow the doc. Update the snippet to match `generateNodePaths` behavior. See `docs/nested-claude-execution.md:262`.
