# Review: 6e1a97a

**Commit:** 6e1a97a6716dc37dcb1686e264c215e6fa21f96d
**Message:** fix: address Codex review issues
**Date:** 2026-01-05 23:15:19 UTC

## Feedback

`src/core/claude-executor.ts`: `convertTools()` now ignores legacy `parameters` entirely. Any JS consumer (or older code) still passing `parameters` will silently get an empty `input_schema`, causing Claude to accept no args or reject calls. Consider supporting a deprecated fallback (map `parameters` -> `input_schema`) or explicitly validate/throw when `input_schema` is missing but `parameters` exists.
