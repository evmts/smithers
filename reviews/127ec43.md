# Review: 127ec43

**Commit:** 127ec43b81fc0978327e2791ed69a20d3f9a40ca
**Message:** 🐛 fix: dedupe tool names and improve TS config error messaging
**Date:** 2026-01-06 00:08:07 UTC

## Feedback

- `src/core/execute.ts:574`: collision dedupe removes only the first matching MCP tool; if multiple MCP servers expose the same tool name, duplicates can still be sent. Remove all tools with that name (e.g., `tools = tools.filter(t => t.name !== inlineTool.name)` before pushing the inline tool).
