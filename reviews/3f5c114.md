# Review: 3f5c114

**Commit:** c5f0570d8791f3dace3f9d7c45176927f6bd9e5f
**Message:** 📝 docs: add state management guide
**Date:** 2026-01-05 23:38:11 UTC

## Feedback

- `docs/concepts/state-management.mdx`: The “stale closure” example is misleading; `useState` avoids the bug with a functional update (`setResults(prev => ({ ...prev, [topic]: data }))`). Revise the example to show the correct `useState` pattern, then explain why Zustand still helps.
- `docs/concepts/state-management.mdx`: In “Complex State Machines,” `addResult` uses `get().plan.tasks.length` without guarding `plan === null`. Add a null check or initialize `plan` with a `tasks` array to avoid a runtime error in the example.
