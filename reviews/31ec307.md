# Review: 31ec307

**Commit:** 31ec307c1d4590c8ad50160831df38ea839d035d
**Message:** 🐛 fix(examples): add optional chaining to array methods
**Date:** 2026-01-06 02:55:15 UTC

## Feedback

The optional chaining doesn’t fully prevent crashes: `initialPlan?.steps?.map(...).join('\n')` (and similar) will still throw when `steps` is undefined because `.join` is called on `undefined`. Use `steps?.map(...)?.join('\n')` or `(steps ?? []).map(...).join('\n')` (same for `testCases`, `apis`, `risks` and for `refinedPlan`).
