# Review: 18b75bc

**Commit:** 18b75bc2e988b6ff2b4f6795127039a2bf2f53c8
**Message:** 📝 docs(intro): add features list, CLI section, reorganize structure
**Date:** 2026-01-06 02:01:05 UTC

## Feedback

`docs/introduction.mdx`: The workflow example has API/state bugs. `useWorkflowStore` only defines `phase`, `prompt`, `initialPlan`, `refinedPlan`, `nextPhase`, but later destructures `setPhase` and `setPrompt`, and calls them (`setPhase('cancelled')`, `setPrompt(...)`). Add these setters (or update the example to use `set` directly) so the sample code is accurate.

`docs/introduction.mdx`: The `phases` array used by `nextPhase()` omits values that exist in `WorkflowPhase` and in the switch (`refined-review`, `api-impl`, etc.). `refined-review` is in the union but never added to the sequence, so it can’t be reached. Align the phase list with the union/switch or remove unused phases.
