# Review: 46b3762

**Commit:** 46b3762ecbff77e06e10c0440d3c5e7eef796c3f
**Message:** 📝 docs: add state management guide
**Date:** 2026-01-05 23:25:20 UTC

## Feedback

- `docs/concepts/state-management.mdx`: The “stale closure” example is misleading; `useState` can avoid the bug with a functional update (`setResults(prev => ({ ...prev, [topic]: data }))`). Consider revising to show the correct `useState` pattern and then explain why Zustand still helps.
- `docs/concepts/state-management.mdx`: In “Complex State Machines,” `addResult` calls `get().plan.tasks.length` without guarding for `plan === null`. Add a null check or initialize `plan` to a shape with `tasks` to avoid runtime errors in the example.
