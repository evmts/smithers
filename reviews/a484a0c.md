# Review: a484a0c

**Commit:** a484a0cfadb7a540e96429ca3e15acafeb722b1f
**Message:** docs: rewrite README with comprehensive examples and clear value proposition
**Date:** 2026-01-05 21:25:08 UTC

## Feedback

- `README.md:82`, `README.md:482`, `README.md:515` document `--props` for `smithers run`/`smithers plan`, but the CLI doesn’t define or consume that flag (`src/cli/commands/run.ts:12-39`, `src/cli/commands/plan.ts:10-27`). Either implement `--props` and wire it into compilation/rendering, or remove it from the docs.
- `README.md:484` documents `--auto-approve` (`-y`), but the CLI uses `--yes` as the long flag (`src/cli/commands/run.ts:15`). Update the README or add `--auto-approve` as an alias.
- `README.md:487` says `--timeout` is milliseconds, but the CLI defines it as seconds and multiplies by 1000 (`src/cli/commands/run.ts:19`, `src/cli/commands/run.ts:120`). Align units in docs or change the code to accept ms.
- `README.md:523-530` claims `smithers init` creates `tsconfig.json`, `src/agent.tsx`, and `.env.example`, but `smithers init` only writes `agent.mdx` (template) and `package.json` (`src/cli/commands/init.ts:25-205`). Either generate those files or adjust the README.
