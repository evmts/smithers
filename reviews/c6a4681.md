# Review: c6a4681

**Commit:** c6a4681b5ab62a9a99436a5a73e90a392ed00e3b
**Message:** ✅ test: add comprehensive renderer test coverage (32 tests)
**Date:** 2026-01-06 01:08:42 UTC

## Feedback

- `src/core/claude-cli-executor.ts`: You pass the prompt via `--prompt` but your own docs show a positional prompt (`claude --print --output-format text "Your prompt here"` in `docs/components/claude-cli.mdx`). If the CLI doesn’t accept `--prompt`, this will fail at runtime. Action: either pass the prompt as the final positional arg or update docs + add a guard/compat check for the flag.
- `src/core/execute.ts`: `mockMode` only forces mock when `true`; it does not let callers force real execution when `SMITHERS_MOCK_MODE=true` in env. This makes `executePlan({ mockMode: false })` ineffective in that scenario. Action: treat `mockMode` as an override in both directions (e.g., if `mockMode === false`, ignore env mock and require real mode).
