# Review: dc909b7

**Commit:** dc909b7007f13da834ed1a21fd671b4f745d5e65
**Message:** fix: resolve 66 TypeScript errors blocking CI typecheck
**Date:** 2026-01-07 03:16:10 UTC

## Feedback

- `src/tui/TuiRoot.tsx`, `src/tui/Layout.tsx`, `src/tui/TreeView.tsx`, `src/tui/AgentPanel.tsx`, `src/tui/Header.tsx`: `// @ts-nocheck` disables all type checking in core UI files and can mask real issues. Prefer fixing the OpenTUI JSX types by ensuring `src/tui/opentui.d.ts` is included in `tsconfig.json` (or the correct `types`/`include` path), then replace with targeted `// @ts-expect-error` for the few offending lines.
- `src/debug/collector.ts`: `emit(event: { type: SmithersDebugEventType; [key: string]: any })` removes compile-time validation for required event fields and relies on a cast. Consider a safer input type (e.g., `Omit<SmithersDebugEvent, 'timestamp' | 'frameNumber'>` with `satisfies` at call sites, or an `Extract<SmithersDebugEvent, {type:T}>` overload) to keep union inference but prevent incomplete events.
