# Review: 95e770d

**Commit:** 78aa1e29eb626bead0a880dfada22a0652259172
**Message:** fix: address Codex review issues
**Date:** 2026-01-05 23:38:20 UTC

## Feedback

- `src/core/claude-executor.ts`: `convertTools()` now ignores legacy `tool.parameters` without warning. Any existing callers still passing `parameters` will silently send an empty `input_schema`, which can break tool invocation. Add a deprecated fallback (`parameters` → `input_schema`) or throw/validate when `input_schema` is missing but `parameters` is present.
- `src/core/claude-executor.ts`: `input_schema` is passed through unvalidated. If a caller supplies a schema without `type: "object"` (or an invalid JSON Schema), Anthropic may reject the tool. Consider validating or normalizing to a strict object schema before sending.
