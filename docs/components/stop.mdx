---
title: Stop
description: Signal workflow termination
---

# Stop

```tsx
<Stop reason="Task completed successfully" />
```

## Props

| Prop | Type | Description |
|------|------|-------------|
| `reason` | `string` | Reason for stopping |

## Conditional Stop

```tsx
function ConditionalWorkflow() {
  const [status, setStatus] = useState("running");

  return (
    <SmithersProvider db={db} executionId={executionId}>
      <If condition={status === "running"}>
        <Claude
          onFinished={(result) => {
            if (result.output.includes("CRITICAL")) setStatus("error");
            else if (result.output.includes("DONE")) setStatus("complete");
          }}
        >
          Process the task.
        </Claude>
      </If>

      <If condition={status === "error"}>
        <Stop reason="Critical error detected" />
      </If>

      <If condition={status === "complete"}>
        <Stop reason="Task completed" />
      </If>
    </SmithersProvider>
  );
}
```

## Related

<CardGroup cols={2}>
  <Card title="Human" icon="user" href="/components/human">
    Human approval
  </Card>
  <Card title="SmithersProvider" icon="sitemap" href="/components/smithers-provider">
    Workflow control
  </Card>
</CardGroup>
