---
title: Subagent
description: Named execution boundaries for Claude grouping
---

# Subagent

```tsx
<Subagent name="researcher">
  <Claude>Research the topic thoroughly.</Claude>
</Subagent>
```

Wraps child components in a named execution boundary for organization, debugging visibility, and logging.

<Note>
The `parallel` prop is metadata-only. For actual parallel execution, use `<Parallel>`.
</Note>

## Props

| Prop | Type | Default | Description |
|------|------|---------|-------------|
| `name` | `string` | — | Name for debugging and logging |
| `parallel` | `boolean` | `false` | Metadata hint (does not enforce parallelism) |
| `children` | `ReactNode` | — | Components to group |

## Named Groups

```tsx
<Subagent name="analysis">
  <Claude allowedTools={["Read", "Grep"]}>
    Analyze the codebase structure.
  </Claude>
</Subagent>

<Subagent name="implementation">
  <Claude allowedTools={["Read", "Edit", "Write"]}>
    Implement the feature.
  </Claude>
</Subagent>
```

## With Parallel Execution

Use `<Parallel>` inside Subagent for actual parallel execution:

```tsx
<Subagent name="research">
  <Parallel>
    <Claude>Research frontend optimization.</Claude>
    <Claude>Research backend optimization.</Claude>
  </Parallel>
</Subagent>
```

## Best Practices

- **Use descriptive names** — `security-audit`, `api-integration`, not `step1`
- **Use Parallel for independent tasks** — wrap truly independent work in `<Parallel>`
- **Group related operations** — keep logically related Claude calls together

## Related

<CardGroup cols={2}>
  <Card title="Parallel" icon="layer-group" href="/components/parallel">
    Actual parallel execution
  </Card>
  <Card title="Phase" icon="layer-group" href="/components/phase">
    Phase-based workflow control
  </Card>
</CardGroup>
