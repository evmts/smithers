# Anthropic API Key
# Get your API key from https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-api03-...

# Enable mock mode for testing (optional)
# When set to "true", execution will use mock responses instead of calling the Claude API
# This is useful for running tests without incurring API costs
SMITHERS_MOCK_MODE=false
